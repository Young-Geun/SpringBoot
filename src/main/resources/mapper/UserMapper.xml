<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="choi.web.springboot.repository.UserRepository">

    <select id="selectUser" resultType="choi.web.springboot.domain.User">
        SELECT user_id,
               user_email,
               user_password,
               user_password user_password_cert,
               user_name,
               user_status
        FROM user_info
        WHERE user_email = #{userEmail}
        AND user_password = #{userPassword}
    </select>

    <select id="selectExistUser" resultType="choi.web.springboot.domain.User">
        SELECT user_id,
               user_email,
               user_password,
               user_password user_password_cert,
               user_name,
               user_status
        FROM user_info
        <where>
            <if test="userEmail != null and userEmail !=''">
                and user_email = #{userEmail}
            </if>
        </where>
    </select>

    <insert id="registUser" parameterType="choi.web.springboot.domain.User">
        INSERT INTO user_info (
            user_id,
            user_email,
            user_password,
            user_name,
            user_status
        )
        VALUES (
            (SELECT MAX(user_id) + 1 FROM user_info),
            #{userEmail},
            #{userPassword},
            #{userName},
            'Y'
        )
    </insert>

    <update id="editUser" parameterType="choi.web.springboot.domain.User">
        UPDATE user_info
        SET user_name = #{userName},
            user_password = #{userPassword}
        WHERE user_id = #{userId}
    </update>

</mapper>